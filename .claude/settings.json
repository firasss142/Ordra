{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -qE 'rm -rf|DROP TABLE|git push --force|DELETE FROM.*WHERE'; then echo 'BLOCKED: Dangerous command. Run manually if intentional.' >&2; exit 2; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "npx tsc --noEmit 2>&1 | head -20 || true"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '— Session checkpoint: [ ] tests pass [ ] typecheck clean [ ] git committed —'"
          }
        ]
      }
    ]
  }
}