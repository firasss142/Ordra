"use client";

/**
 * Superseded by QueuePage — kept for reference only.
 * The active queue page is src/components/queue/QueuePage.tsx.
 */
export function AgentQueue(_props: { marketId: string; maxAttempts?: number }) {
  return null;
}
